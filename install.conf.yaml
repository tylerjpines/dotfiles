- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.dir_colors:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.vimrc:
    ~/com.googlecode.iterm2.plist:
    ~/.zshrc:
    ~/.oh-my-zsh:
    ~/.exercism:

- shell:
  - [git submodule update --init --recursive, Installing submodules]