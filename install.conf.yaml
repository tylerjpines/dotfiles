- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.exercism.json:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.mackup:
    ~/.mackup.cfg:
    ~/.vimrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]